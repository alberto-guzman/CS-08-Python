# ask user for numbers and save each one to numbers.txt
# Read all the numbers fromt that file and add them
# print the sum


def main():
    number = int(input('How many number?'))
    for i in range(number)
        run_num = input('What is the number?')
        run_num = + run_num
    number_file = open('numbers.txt",'w')


birthday_file = open('user.bd', 'w')
    birthday_file.write(birthday + '\n')
    birthday_file.close()

main()


# prof code sentinal code


def main():

    number = input('What is the number?')
    num_file = open('numbers.txt', 'w')

    while number != '':
        number = int(number)
        num_file.write(str(number) + '/n')
        number = input('What number?')
    num_file.close()


main()
